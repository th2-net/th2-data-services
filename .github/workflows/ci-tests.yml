name: CI tests

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Test with pytest
        run: |
          python unittestV5.py
          python unittestV6.py
#        run: |
#          pip install pytest
#          pytest -k "not test_parent_events_tree_local and not test_data_source_local and not test_data_local and not test_events_tree_local and not test_adapter_sse_local"
